<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hl.fruitmall.mapper.MerchantInfoMapper">

    <update id="updateByField">
        UPDATE merchant_info SET
        update_time = CURRENT_TIMESTAMP,
        <if test="field == 'status'.toString">
            status = #{value}
        </if>
        WHERE id = #{id}
    </update>
    <select id="getList" resultType="com.hl.fruitmall.entity.vo.MerchantVO">
        SELECT id,user_id,id_card,name,positive,negative,status FROM merchant_info WHERE status = #{status} LIMIT 10 OFFSET #{cur}
    </select>
    <select id="getTotal" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM merchant_info WHERE status = #{status}
    </select>
</mapper>